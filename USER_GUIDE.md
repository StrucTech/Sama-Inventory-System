# 📋 دليل الاستخدام - نظام Sama لإدارة المخزون

## 👨‍💻 للمطور (أنت)

### 🚀 التطوير اليومي
```bash
# 1. تعديل الكود كما تريد
# 2. اختبار التغييرات محلياً
python main.py

# 3. حفظ التغييرات
git add .
git commit -m "وصف التحديث"
git push
```

### 🔖 إنشاء إصدار جديد
```bash
# طريقة سريعة - شغل السكريپت
create_release.bat
# واكتب رقم الإصدار الجديد (مثل v1.0.1)

# أو يدوياً
git tag -a v1.1.0 -m "وصف الإصدار الجديد"
git push origin v1.1.0
```

### 🏗️ بناء محلي (للاختبار)
```bash
# بناء النسخة المستقلة محلياً
build.bat
# النتيجة في مجلد dist/
```

### ⚙️ إعداد Google Sheets (مرة واحدة)
1. انسخ `config/credentials_example.json` إلى `config/credentials.json`
2. عدل المعلومات بمعلومات حسابك من Google Cloud Console
3. لا تشارك هذا الملف مع أحد!

---

## 👤 للمستخدم النهائي

### 📥 التحميل والتثبيت
1. **اذهب إلى**: https://github.com/StrucTech/Sama-Inventory-System/releases
2. **حمل أحدث إصدار**: `Sama-Inventory-System-vX.X.X.zip`
3. **استخرج الملفات** في مجلد على سطح المكتب
4. **شغل البرنامج**: `نظام إدارة المخزون.exe`

### 🔄 التحديث التلقائي
- البرنامج يتحقق من التحديثات تلقائياً عند بدء التشغيل
- إذا توفر تحديث، سيعرض نافذة تحديث
- اضغط "تحديث الآن" وسيتم التحديث تلقائياً
- **لا حاجة لإعادة تحميل أو تثبيت يدوي!**

### 🛠️ الإعداد الأولي
1. **أول مرة تشغيل**: سيطلب منك معلومات Google Sheets
2. **اتبع التعليمات** الظاهرة على الشاشة
3. **اختبر الاتصال** قبل البدء

### 📱 الاستخدام اليومي
- **تسجيل الدخول** بحسابك
- **عرض المخزون** الحالي
- **إضافة/تعديل/حذف** المنتجات
- **تتبع المشاريع** والمستخدمين
- **مراجعة التقارير** والإحصائيات

---

## 🔄 دورة النشر والتحديث

### من جانب المطور:
```
تعديل الكود → اختبار → commit → push → إنشاء tag → 
البناء التلقائي (GitHub Actions) → نشر الإصدار
```

### من جانب المستخدم:
```
تشغيل البرنامج → فحص التحديثات → تحديث تلقائي → 
استخدام الميزات الجديدة
```

---

## 📂 ملفات مهمة للمطور

| الملف | الغرض |
|-------|--------|
| `main.py` | نقطة البداية |
| `build.bat` | بناء محلي |
| `create_release.bat` | إنشاء إصدار جديد |
| `setup_token.bat` | إعداد Git (مرة واحدة) |
| `config/credentials.json` | معلومات Google API (سري!) |
| `.github/workflows/` | إعدادات البناء التلقائي |

---

## 🎯 نصائح مهمة

### للمطور:
- **اختبر محلياً** قبل النشر
- **استخدم أرقام إصدارات منطقية** (v1.0.0 → v1.0.1 → v1.1.0)
- **اكتب وصف واضح** لكل تحديث
- **احتفظ بنسخة احتياطية** من credentials.json

### للمستخدم:
- **اقبل التحديثات التلقائية** للحصول على أحدث الميزات
- **اعمل نسخة احتياطية** من بياناتك في Google Sheets
- **تواصل مع المطور** عند مواجهة مشاكل

---

## 🆘 حل المشاكل الشائعة

### مشاكل المطور:
- **Git authentication**: شغل `setup_token.bat`
- **فشل البناء**: راجع GitHub Actions logs
- **مشاكل credentials**: تأكد من صحة ملف JSON

### مشاكل المستخدم:
- **لا يعمل البرنامج**: تأكد من Windows Defender/Antivirus
- **فشل التحديث**: شغل كـ Administrator
- **مشاكل Google Sheets**: تحقق من الاتصال بالإنترنت

---

## 🔗 روابط سريعة

- **Repository**: https://github.com/StrucTech/Sama-Inventory-System
- **Releases**: https://github.com/StrucTech/Sama-Inventory-System/releases
- **Actions**: https://github.com/StrucTech/Sama-Inventory-System/actions
- **Issues**: https://github.com/StrucTech/Sama-Inventory-System/issues